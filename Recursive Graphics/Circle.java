public class Circle implements Shape {
 
    double x, y; //posn
    double radius;//if its a class to create an instance we need a constructor
    //it should take in a double to rep x,y, and radius 
    public Circle(double x, double y, double radius) {
    this.x = x;
    this.y = y; 
    this.radius = radius; 
    
    }

    public double getCenterX() {
    return x;
    }
public double getCenterY();

public double perimeter

}